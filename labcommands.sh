echo "git checkout labpc"
git checkout labpc

echo "pulling labpc"
git pull origin labpc

echo "git checkout master"
git checkout master
echo "git pull origin master "

git pull origin master

echo "git checkout labpc"
git checkout labpc

echo "git merge master"
git merge master

echo "git push origin labpc"
git push origin labpc
